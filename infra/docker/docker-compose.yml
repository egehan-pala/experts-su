version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg15
    container_name: experts-su-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: experts_su
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
